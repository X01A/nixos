---
kind: pipeline
name: default
type: exec

trigger:
  branch:
  - master

steps:
- name: Building
  environment:
    CACHIX_TOKEN:
      from_secret: CACHIX_TOKEN
  commands:
  - cachix authtoken $CACHIX_TOKEN
  - bash build-cache.bash

node:
  os: nixos
  arch: amd64
